# Pipleine Overview 


UNION of Web data and Browser Data datasets

Write to Ontology to be used in Workshop



To classify domains, I used a code repository (SQL) to select all new domains 
I then realized I had to make a temp and new, as on each refresh the entire table will be wiped out (CREATE OR REPLACE)
